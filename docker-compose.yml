version: '3.9'

services:
  web:
    build: .
    volumes:
      - ./app/:/usr/src/app/
    ports:
      - "8080:8080"
    env_file:
      - .env_example
    depends_on:
      - db
    links:
      - db:db
  db:
    restart: always
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: 'drf_forum'
      POSTGRES_USER: 'admin'
      POSTGRES_PASSWORD: 'admin'


volumes:
  postgres_data: